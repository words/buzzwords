{
  "name": "buzzwords",
  "version": "0.1.2",
  "description": "List of (possible) English buzzword words",
  "license": "MIT",
  "keywords": [
    "buzzword",
    "word",
    "list"
  ],
  "author": "Titus Wormer <tituswormer@gmail.com>",
  "dependencies": {
    "datalist-interface": "^0.1.0"
  },
  "devDependencies": {
    "eslint": "^0.8.0",
    "istanbul": "^0.3.0",
    "jscs": "^1.7.0",
    "mocha": "^1.20.0",
    "plain-text-data-to-json": "^0.1.1",
    "scraperjs": "^0.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wooorm/buzzwords.git"
  },
  "scripts": {
    "test": "node_modules/.bin/_mocha --reporter spec --check-leaks -u exports test.js",
    "test-travis": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha --report lcovonly -- --reporter spec --check-leaks -u exports test.js",
    "lint": "npm run lint-api && npm run lint-test && npm run lint-style",
    "lint-api": "node_modules/.bin/eslint index.js scrape-data.js build-data.js build-support.js --env node --env browser --rule 'quotes: [2, single]'",
    "lint-test": "node_modules/.bin/eslint test.js --env node --env mocha --rule 'quotes: [2, single]'",
    "lint-style": "node_modules/.bin/jscs index.js scrape-data.js build-data.js build-support.js test.js --reporter=inline",
    "scrape-data": "node scrape-data.js",
    "build-data": "node build-data.js",
    "build-support": "node build-support.js",
    "coverage": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- -- test.js",
    "prepublish": "npm run scrape-data && npm run build-data && npm run build-support",
    "make": "npm run lint && npm run coverage"
  }
}
